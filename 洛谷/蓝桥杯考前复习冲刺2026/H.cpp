#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

map<int, int> mp;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

    int m;;
    cin >> m;
    while(m--){
        int op, x;
        cin >> op >> x;
        if(op == 1){
            
        }
        else {
            mp.insert({x, 1});
            
        }
    }
    

    return 0;
}