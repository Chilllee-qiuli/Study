#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int T;

void solve_one();

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

    cin >> T;
    while(T--) solve_one();

	return 0;
}

void solve_one(){

}