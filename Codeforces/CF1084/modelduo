#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int t;


void solve_one(){

}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);
    cin >> t;
    while(t--) solve_one();

	return 0;
}
