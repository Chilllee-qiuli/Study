#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
using namespace std;
int t;

int main() {
   // freopen("1.in", "r", stdin);
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

    cin >> t;
    while (t--) {
        ll x;
        cin >> x;
        cout << x * 2 << endl;
    }
    return 0;
}